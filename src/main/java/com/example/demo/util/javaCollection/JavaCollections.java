package com.example.demo.util.javaCollection;

public class JavaCollections {
    /**
     * 集合类存放在java.util包中
     * 主要有三种：set（集）、list（列表包含Queue）和 map（映射）
     *
     * 1.Collection：Collection是集合List、Set、Queue的最基本的接口
     * 2.Iterator：迭代器，可以通过迭代器遍历集合中的数据
     * 3.Map：是映射表的基础接口
     */
}
